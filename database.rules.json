{
  "rules": {
    "geolocations": {
      ".read": true,
      "$uid": {
        ".write": "$uid === auth.uid"
      }
    },
    "profiles": {
      ".read": true,
      "$uid": {
        ".write": "$uid === auth.uid"
      }
    }
  }
}
